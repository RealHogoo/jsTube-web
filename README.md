# jsTube Web

React/Vite frontend for the current JsTube media service.

This repository intentionally contains source code only. Do not commit production `.env` files, tokens, build output, or private keys.

## Local

```sh
npm ci
cp .env.example .env
npm run dev
```
